apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "enclaveid.fullname" . }}-test-connection"
  labels:
    {{- include "enclaveid.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "enclaveid.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

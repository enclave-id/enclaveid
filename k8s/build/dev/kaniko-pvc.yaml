kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: build-context-claim
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
  storageClassName: local-storage

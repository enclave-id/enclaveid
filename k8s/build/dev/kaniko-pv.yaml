apiVersion: v1
kind: PersistentVolume
metadata:
  name: build-context
  labels:
    type: local
spec:
  capacity:
    storage: 10Gi
  accessModes:
    - ReadWriteMany
  storageClassName: local-storage
  hostPath:
    path: ${HOST_PATH}

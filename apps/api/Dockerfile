# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build api`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t api`.
FROM node:20.11.1-alpine3.18

RUN npm install -g pnpm@9.0.6

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /kaniko/buildcontext/

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.

# TODO: Might wanna exclude devDependencies but we need nx to build the app...
RUN pnpm install

# Generate Prisma client
RUN pnpm exec nx run api:prisma-generate

RUN pnpm exec nx run api:build --configuration=development

RUN ls -la

RUN ls -la dist/apps/api

COPY dist/apps/api /api

CMD [ "node", "api" ]

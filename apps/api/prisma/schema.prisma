generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model GoogleSearch {
  id    Int      @id @default(autoincrement())
  searchString String
  time  DateTime
}
